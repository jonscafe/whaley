# Example Web Challenge
id: example-web
name: "Example Web Challenge"
category: web
description: "A simple web exploitation challenge"

# Internal ports to expose (will be mapped to random external ports)
ports:
  - 80

# Instance timeout in seconds (default: 3600 = 1 hour)
timeout: 3600
